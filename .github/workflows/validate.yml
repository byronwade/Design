name: Validate design contract

on:
  pull_request:
  push:
    branches: [main]

permissions:
  contents: read

jobs:
  validate:
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-latest, windows-latest]
    runs-on: ${{ matrix.os }}
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 22
      - run: npm install --ignore-scripts
      - name: Validate contract and Google DESIGN.md
        run: npm run validate:strict
      - name: Run resolver, installer, synchronization, and safety tests
        run: npm test
      - name: Pack, install, and execute the published package shape
        run: npm run smoke
